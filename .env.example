# API Port Binding Configuration
# Port for the server to listen on
HOST_PORT=80

# Email Server Configuration
# Replace these values with your actual SMTP server details
# SMTP server hostname (e.g., smtp.gmail.com, smtp.outlook.com, smtp.example.com)
MAIL_DOMAIN=smtp.example.com
# Port for the SMTP server to receive emails
MAIL_PORT=25
# SMTP password for the recipient email account
# For Gmail: Use an App Password instead of your regular password
# For other providers: Use the SMTP password provided by your email provider
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password


# API key encryption configuration
API_KEY_SECRET=your-api-key-secret
API_KEY_CIPHER=your-api-key-cipher
API_KEY_IV_LENGTH=12
API_KEY_SALT_LENGTH=16
GCM_TAG_LENGTH=128
PBKDF2_ITERATIONS=100000